Change the product name from YieldKey to QuestKey everywhere it appears on the site — headings, subheadlines, footer, meta title, and any other references. Do not miss any instances.

Redesign the logo. The logo should be the letter Q where the tail/tip of the Q is shaped like a key. Keep the coral/salmon red color for the logo. It should be simple and clean enough to read at small sizes in the nav bar. Replace the current YieldKey logo with this new QuestKey logo everywhere it appears including the footer.

Rename the AI agents so each name starts with a letter that matches their role. Miles (Underwriting) needs to start with U — rename to something like “Uri” or “Una.” Go through all six agents and rename them so the first letter of the name matches the first letter of the role: U for Underwriting, A for Acquisitions stays fine (Avery works), R for Risk stays fine (Rhea works), S for Setup stays fine (Sloane works), R for Revenue — rename Kai to something starting with R like “Ren” or “Reva,” and P for Portfolio — rename Theo to something starting with P like “Penn” or “Pia.” Update the names in both the agent cards and anywhere else they are referenced on the page.

Add a feature to the hero section where a user can input either a specific property address or a market/city name. Replace or augment the current static demo with an input field that has placeholder text like “Enter an address or market (e.g. Austin TX, 1248 Oakwood Ave)” so visitors can see the product accepts both property-level and market-level analysis. This does not need to be functional yet — it can show a coming soon message or scroll down to the demo section when submitted. The point is to make it feel interactive and show that the product handles both individual properties and market-level research.

Add a hamburger menu icon (three small horizontal dashes) to the top right of the mobile nav bar. When tapped it should open a simple mobile menu with anchor links that scroll to each major section of the page: How It Works, AI Agents, Features, and Get Early Access. This lets mobile users jump to any section without scrolling the entire page. On desktop and tablet the nav can show these links inline in the header instead of behind a hamburger. Make sure the menu closes when a link is tapped and the page scrolls smoothly to the selected section.​​​​​​​​​​​​​​​​