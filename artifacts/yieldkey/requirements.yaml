packages:
  "framer-motion": "For premium smooth animations and page transitions"
  "lucide-react": "For beautiful icons across the dashboard"
  "react-hook-form": "For robust form state management"
  "@hookform/resolvers": "For Zod schema validation integration"
  "zod": "For runtime type safety and form validation"
  "clsx": "For conditional class joining"
  "tailwind-merge": "For conflict-free tailwind class merging"
  "date-fns": "For readable date formatting"

images: []

notes:
  - Auth uses JWT tokens stored in localStorage ('yk_token')
  - Protected routes verify token via GET /api/auth/me
  - custom fetch wrapper handles attaching the Authorization header
